// 消息总线，组件通信
import Vue from 'vue'

const event = new Vue();

module.exports = {
	// emit: (type, data) => {
	// 	uni.$emit(type, data);
	// },
	// on: (type, fn) => {
	// 	uni.$on(type, data => {
	// 		fn(data);
	// 	});
	// },
}
